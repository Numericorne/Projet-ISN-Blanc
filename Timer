def Timer():
    global temps,minutes
    if flag > 0 :
        temps+= 0.01
        if temps % 60 == 0:
            print("yo")
            minutes+=1
            temps = 0
        fen.after(1,Timer)
        TimerPrint.configure(text=(int((temps*100)))/100)
